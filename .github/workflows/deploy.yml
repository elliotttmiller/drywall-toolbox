name: Deploy Drywall Toolbox to HostGator

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'
      - 'wp/**'
      - '.htaccess'
      - 'index.php'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (no files uploaded)'
        required: false
        default: false
        type: boolean
      remote_root:
        description: 'Remote site root path'
        required: false
        default: '/home1/benconklin/public_html/website_a246e6a8'
        type: string

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build React Frontend
    runs-on: ubuntu-latest
    permissions:
      contents: read

    outputs:
      build-cache-key: ${{ steps.cache-key.outputs.key }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: cd frontend && npm ci --prefer-offline --no-audit --no-fund

      - name: Build frontend (webpack → ../dist/)
        run: cd frontend && npm run build
        env:
          NODE_ENV: production
          # Legacy CRA-style vars (src/services/api.js, src/api/wordpress.js)
          REACT_APP_WP_BASE_URL:            ${{ secrets.REACT_APP_WP_BASE_URL }}
          REACT_APP_WC_BASE_URL:            ${{ secrets.REACT_APP_WC_BASE_URL }}
          REACT_APP_WC_CONSUMER_KEY:        ${{ secrets.REACT_APP_WC_CONSUMER_KEY }}
          REACT_APP_WC_CONSUMER_SECRET:     ${{ secrets.REACT_APP_WC_CONSUMER_SECRET }}
          # New VITE_* vars for headless WP + WC architecture
          VITE_WP_API_BASE:                 ${{ secrets.VITE_WP_API_BASE }}
          VITE_WC_API_BASE:                 ${{ secrets.VITE_WC_API_BASE }}
          VITE_JWT_ENDPOINT:                ${{ secrets.VITE_JWT_ENDPOINT }}
          VITE_SITE_URL:                    ${{ secrets.VITE_SITE_URL }}
          VITE_APP_ENV:                     production
          VITE_WC_AUTH_USER:                ${{ secrets.VITE_WC_AUTH_USER }}
          VITE_WC_AUTH_PASS:                ${{ secrets.VITE_WC_AUTH_PASS }}
          # Legacy WooCommerce vars
          VITE_WOOCOMMERCE_STORE_URL:       ${{ secrets.VITE_WOOCOMMERCE_STORE_URL }}
          VITE_WOOCOMMERCE_CONSUMER_KEY:    ${{ secrets.VITE_WOOCOMMERCE_CONSUMER_KEY }}
          VITE_WOOCOMMERCE_CONSUMER_SECRET: ${{ secrets.VITE_WOOCOMMERCE_CONSUMER_SECRET }}
          PUBLIC_URL:                       ${{ secrets.PUBLIC_URL }}

      - name: Validate build output
        run: |
          DIST="dist"
          echo "── dist contents ──"
          find "$DIST" -type f | sort
          if [ ! -f "$DIST/index.html" ]; then
            echo "ERROR: dist/index.html not found — build may have failed."
            exit 1
          fi
          if [ ! -f "$DIST/asset-manifest.json" ]; then
            echo "WARNING: asset-manifest.json not found."
          fi
          echo "✓ Build output validated."

      - name: Prune source maps before upload
        run: |
          echo "Pruning .map files from dist/..."
          find dist/ -type f -name "*.map" -print -delete || true
          echo "✓ Pruning complete."

      - name: Generate build artifact key
        id: cache-key
        run: echo "key=build-${{ github.sha }}" >> "$GITHUB_OUTPUT"

      - name: Upload dist as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ github.sha }}
          path: dist/
          retention-days: 1
          if-no-files-found: error

  deploy:
    name: FTP Deploy to HostGator
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read

    steps:
      - name: Checkout repository (wp/ + root files)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          sparse-checkout: |
            wp
            .htaccess
            index.php

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: build-${{ github.sha }}
          path: dist/

      - name: Validate deploy structure
        run: |
          echo "── dist ──"
          ls -la dist/ || true
          echo "── wp/wp-content ──"
          ls -la wp/wp-content/ || true
          echo "── root files ──"
          ls -la .htaccess index.php 2>/dev/null || true

      - name: "Dry-run: display what would be deployed"
        if: "${{ github.event.inputs.dry_run == 'true' }}"
        run: |
          echo "Dry-run mode: displaying deploy plan."
          echo
          echo "Would deploy .htaccess + index.php → ${{ github.event.inputs.remote_root }}/"
          echo "Would deploy dist/ → ${{ github.event.inputs.remote_root }}/dist/"
          echo "Would deploy wp/wp-content/ → ${{ github.event.inputs.remote_root }}/wp/wp-content/"

      - name: Stage root files for deployment
        run: |
          mkdir -p root-deploy
          cp .htaccess index.php root-deploy/
          echo "── root-deploy contents ──"
          ls -la root-deploy/

      - name: Deploy root files (.htaccess, index.php) via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server:   ${{ secrets.HOSTGATOR_FTP_HOST }}
          username: ${{ secrets.HOSTGATOR_FTP_USER }}
          password: ${{ secrets.HOSTGATOR_FTP_PASS }}
          protocol: ftps
          port:     ${{ secrets.HOSTGATOR_FTP_PORT }}
          server-dir: /home1/benconklin/public_html/website_a246e6a8/
          local-dir:  ./root-deploy/
          exclude: |
            **/.git*
            **/.github/**
          dangerous-clean-slate: false
          log-level: verbose
          dry-run: "${{ github.event.inputs.dry_run == 'true' }}"
          timeout: 60000

      - name: Deploy dist/ via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server:   ${{ secrets.HOSTGATOR_FTP_HOST }}
          username: ${{ secrets.HOSTGATOR_FTP_USER }}
          password: ${{ secrets.HOSTGATOR_FTP_PASS }}
          protocol: ftps
          port:     ${{ secrets.HOSTGATOR_FTP_PORT }}
          server-dir: /home1/benconklin/public_html/website_a246e6a8/dist/
          local-dir:  ./dist/
          exclude: |
            **/.git*
            **/.github/**
            **/node_modules/**
            **/*.log
            **/.env*
            **/*.map
          dangerous-clean-slate: false
          log-level: verbose
          dry-run: "${{ github.event.inputs.dry_run == 'true' }}"
          timeout: 300000

      - name: Deploy wp/wp-content/ via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server:   ${{ secrets.HOSTGATOR_FTP_HOST }}
          username: ${{ secrets.HOSTGATOR_FTP_USER }}
          password: ${{ secrets.HOSTGATOR_FTP_PASS }}
          protocol: ftps
          port:     ${{ secrets.HOSTGATOR_FTP_PORT }}
          server-dir: /home1/benconklin/public_html/website_a246e6a8/wp/wp-content/
          local-dir:  ./wp/wp-content/
          exclude: |
            **/.git*
            **/.github/**
            **/node_modules/**
            **/*.log
            **/.env*
            **/.gitignore
            **/README.md
            **/twentytwenty*/**
            **/yith-wonder/**
            **/*.bak
            **/*.map
            **/products_catalog.csv
            **/catalog/**
          dangerous-clean-slate: false
          log-level: verbose
          dry-run: "${{ github.event.inputs.dry_run == 'true' }}"
          timeout: 300000

      - name: Deployment summary
        if: always()
        run: |
          echo "────────────────────────────────────────────"
          echo "  Deploy status : ${{ job.status }}"
          echo "  Commit SHA    : ${{ github.sha }}"
          echo "  Triggered by  : ${{ github.actor }}"
          echo "  Timestamp     : $(date -u '+%Y-%m-%dT%H:%M:%SZ')"
          echo "  Dry run       : ${{ github.event.inputs.dry_run == 'true' }}"
          echo "  JS chunks     : $(find dist/assets/js -name '*.js' 2>/dev/null | wc -l) files"
          echo "  WP theme      : wp/wp-content/themes/headless-base/ (active headless theme)"
          echo "  Root .htaccess: .htaccess → website root (React Router + WP routing)"
          echo "────────────────────────────────────────────"

  verify:
    name: Post-Deploy Verification
    runs-on: ubuntu-latest
    needs: deploy
    if: success() && github.event.inputs.dry_run != 'true'

    steps:
      - name: Check dist/index.html on live site
        run: |
          URL="https://drywalltoolbox.com/dist/index.html"
          echo "Probing: $URL"
          for attempt in 1 2 3; do
            code=$(curl -s -o /dev/null -w "%{http_code}" "$URL" || echo "000")
            echo "Attempt $attempt — HTTP $code"
            if [ "$code" = "200" ]; then
              echo "✓ dist/index.html is reachable."
              exit 0
            fi
            [ "$attempt" -lt 3 ] && sleep 15
          done
          echo "⚠ dist/index.html not reachable after 3 attempts (status $code)."
          exit 0

      - name: Check site homepage reachability
        run: |
          code=$(curl -s -o /dev/null -w "%{http_code}" "https://drywalltoolbox.com/" || echo "000")
          echo "Homepage HTTP status: $code"
          if [ "$code" != "200" ] && [ "$code" != "301" ] && [ "$code" != "302" ]; then
            echo "⚠ Homepage returned unexpected status $code."
          else
            echo "✓ Homepage is reachable."
          fi

      - name: Check WordPress REST API
        run: |
          code=$(curl -s -o /dev/null -w "%{http_code}" "https://drywalltoolbox.com/wp/wp-json/" || echo "000")
          echo "WP REST API HTTP status: $code"
          if [ "$code" = "200" ]; then
            echo "✓ WordPress REST API is reachable."
          else
            echo "⚠ WordPress REST API returned status $code."
          fi